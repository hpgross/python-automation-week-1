#/usr/bin/python3

import os
from PIL import Image

# This file will iterate over a series of images
# scaling them down, rotating them by 90 degrees
# and then converting them to a different format

src_dir = "" #The file path for the source directory
prt_dir = "" #The file path for the output directory

for filename in src_dir:
    path = src_dir + "/" + filename
    im = image.open(path)

    #scale

    #rotate

    #convert

    #output